# Token

> AI 世界的字节码。论消耗收费，论容量决定 AI 记性。中英文字效率天差地别。

## 什么是 Token？

Token 是 AI 世界里计费的**最小内存单元**，也是它眼里唯一能看懂的字。AI 不读汉字，不读字母，只读 **Token ID**。

## Token 的特点

对 AI 而言：
- 单词 `"apple"` 可能是 **1 个 Token**
- 但 `"Apples"` 可能也是 **1 个**
- 而 `"AppleS"` 可能就是 **2 个**（因为大小写变化被拆开了）